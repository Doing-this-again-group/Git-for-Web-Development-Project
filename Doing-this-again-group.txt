https://codepen.io/Keyan2016/pen/rNOBepE?editors=1100

Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? - Html that has meaning.
    2. What is HTML used for? - is the standard mark up language for electronic documents.
    3. What is an attribute and where do we put it? - Special words used inside a tag.
    4. What is the h1 tag used for? How many times should I use it on a page? - Is the header header tag and ideally should not be used a lot 
    5. Name two tags that have required attributes - <img> which has src attribute & <a> tag which had href as an attribute
    6. What do we put in the head of our HTML document? - <!Doctype html>
    7. What is an id? - Specifies a specific keyword in the html element. 
    8. What elements can I add an id to? - h1 can be used as a id tag and so can P for paragraph. 
    9. How many times can I use the same id on a page? - only once (unless you like making a life a living hell as an engineer)
    10. What is a class? - Is an attribute that can be used in any html element. 
    11. What elements can I add a class to? - It can be used all html elements.
    12. How many times can I use the same class on a page? - preferrably once. 
    13. How do I get my link to open in a new tab? in html - by adding a target="_blank" attribute 
    14. What is the alt attribute in the image tag used for? - to describe an image especially if someone is blind.       
    15. How do I reference an id? - By adding a # before the id. 
    16. What is the difference between a section and a div-  the section defines sections and a div is the whole container
    17. What is CSS used for? Casscading style sheets is for the design of the markup language (html)
    18. How to we select an element? Example - every h2 on the page - you would select <h2>
    19. What is the difference between a class and an id? Give me an example of when I might use each one - class selectors are started with a (.) where id's can be started with (#)
    20. How do we select classes in CSS? - select elements with a (.) in the beginning before the class. 
    21. How do we select a p element with a single class of “human””? - .human{ }

    
    22. What is a parent child selector? When would this be useful? - the parent child selector is a > symbol and is used to select all elements that have a specific parent element for example:
        header > p { }
    23. How do you select all links within a div with the class of sidebar? - div.sidebar 
    24. What is a pseudo selector? - It is a keyword that gives the special meaning to the selector like (:hover)
    25. What do we use the change the spacing between lines? - Line height property.
    26. What do we use to change the spacing between letters? - Letter - spacing 
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? -  text-transform
    28. How do I add a 1px border around my div that is dotted and black? - 1px - dotted black
    29. How do I select everything on the page? - by putting *
    30. How do I write a comment in CSS? - by using /* my comment */
    31. How do I find out what file I am in, when I am using the command line? - the last command in your terminal should tell you.
    32. Using the command line - how do I see a list of files/folders in my current folder? - ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? - rm
    34. Why should I use version control? - to make sure your work is saved and you can go back and fix it. 
    35. How often should I commit to github? - As often as you can or any time there is a signaficant change. 
    36. What is the command we would use to push our repo up to github? push -u origin <branch name>
    37. Walk me through Lambda's git flow. create a folder you want to add git to:
          
          mdir folder-name
        initialize the git repo:
          git init
        fork the repo from version control that you want to use.
        get the link to the repository after forking it
        clone the repository to your machine:
          git clone git-repository-link
        checkout a new branch:
          git checkout -b branch-name
        when you are ready to stage changes:
          git add .
        when you are ready to commit the changes:
          git commit -m "some message here"
        push the changes to github:
          git push -u branch-name



Stretch Questions

    1. What is the difference between an inline element and a block element? - A block element always starts on a new line and takes up the full width.
    2. What happens when an element is positioned absolutely? hen you position an element absolutely it brings it out of the flow, and acts like it (doesnt exist)
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?  use display: inline-block
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 


